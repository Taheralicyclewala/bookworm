apiVersion: v1                    
kind: Service                     
metadata:
  name: book-info-database         	  
  labels:                         
    app: book-info-mysql		  
    tier: mysql	
spec:
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:                       
    app: book-info-mysql		 
    tier: mysql
