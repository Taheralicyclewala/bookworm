apiVersion: v1                    
kind: Service                     
metadata:
  name: book-rating-database         	  
  labels:                         
    app: book-rating-mysql		  
    tier: mysql	
spec:
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:                       
    app: book-rating-mysql		 
    tier: mysql
